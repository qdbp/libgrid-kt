package model

/**
 * Abstract interface for a grid-entity-plane-tile system solver.
 *
 * Implementers of this interface are responsible for translating the higher-level calls of this
 * interface to a specific modeling language.
 */
interface Solver {

    // TODO need some method to pre-emptively declare the total number of tiles.
}
